/*
Create a function countDigits() that counts the number of digits in a given number 
and prints out the number of digits. 
*/

function countDigits(number) {
    let text = String(number);
    let count = text.length;
    console.log(    count);
}

countDigits(10);